<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.GraphicRuler" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
        <item name="android:tint">@color/white</item>
        <!-- statusBar, navigationBar. -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- prevent from use the auto darkTheme from android10-->
        <item name="android:forceDarkAllowed">false</item>
    </style>
    <style name="EditText.Style" parent="@android:style/Widget.EditText">
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textCursorDrawable">@color/white</item>
    </style>

    <style name="RulerImageView.Style" parent="Theme.GraphicRuler">
        <item name="android:background">@color/white</item>
    </style>
    <style name="ScaleMenuButton.Style" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:drawableEnd">@drawable/ic_scale_menu_arrow_night</item>
    </style>
</resources>